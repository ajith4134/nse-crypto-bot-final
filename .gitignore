# ── Secrets: NEVER commit real keys ──
.env
.env.*
!.env.example
*.key
secrets/
.dashboard_creds

# Python
__pycache__/
**/__pycache__/
*.py[cod]
*.pyc
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# ML artifacts (versioned via DVC/registry, not git)
*.ckpt
*.pt
*.onnx
# P4.8 downloaded model files (kokoro onnx+voices) + corpora — not git (re-fetch via downloads)
models/
models/checkpoints/
nltk_data/
data/raw/
data/processed/
mlruns/
# large model binaries (kokoro voices, etc.)
*.bin

# AutoGluon / symbolic-regression / training run output (generated)
AutogluonModels/
ag_*/
outputs/
*.output

# Julia (pulled in by PySR on first fit)
.julia/

# Compiled native build outputs (rebuilt via native/build.sh)
*.so

# Auto-generated (regenerated by `make index`)
# INDEX.md is committed, but never hand-edited

# OS / editor
.DS_Store
.vscode/
.idea/

# Node / dashboard web app
node_modules/
dashboard/web/node_modules/
dashboard/web/install.log
dist/
build/
# Vite build bundle (regenerated by `npm run build`)
dashboard/static/assets/

# runtime / generated dashboard state
data/cache/
*.log
tunnel.log
dashboard/server.log
state.json
knowledge_state.json
robustness.json

# ── Trading phase runtime state (machine-local; watchlist/toggle/instr cache) ──
trading/state/

# ── Self-hosted OpenAlgo server lives under $HOME but is NOT our project ──
srv/
walkforward.json
intraday_wf.json

# Phase-4 brain/memory: embedded ChromaDB vector store (+ HF embedding-model cache)
memory/.chroma_store/

# Generated dashboard verification screenshot (regenerated by verify_render.py)
dashboard/*.png

# ── Claude Code / tooling (not project source) ──
.claude/
.claude.json

# ── Home-directory files (repo root is $HOME — never track these) ──
.bash_history
.bash_logout
.bashrc
.profile
.cache/
.config/
.local/
.npm/
.ssh/
AutoML_*
.pytensor/
catboost_info/
.dspy_cache/
.mem0/
cloudflared.log

# Freqtrade (crypto engine) — generated config holds the REST password; runtime data is local.
trading/crypto/freqtrade/config.json
trading/crypto/freqtrade/user_data/*.sqlite
trading/crypto/freqtrade/user_data/logs/
trading/crypto/freqtrade/user_data/backtest_results/
trading/crypto/freqtrade/user_data/plot/
trading/crypto/freqtrade/start.sh
trading/crypto/freqtrade/user_data/strategies/lib_*.py
trading/crypto/freqtrade/config_freqai.json
vendor/frequi/node_modules
vendor/frequi/dist

# Brain credential vault (NEVER commit)
trading/state/credentials.enc
trading/state/.vault_key

# Freqtrade runtime data (regenerated; never commit) — closes the user_data sprawl
trading/crypto/freqtrade/user_data/data/
trading/crypto/freqtrade/user_data/backtest_results/
trading/crypto/freqtrade/user_data/hyperopt_results/
trading/crypto/freqtrade/user_data/logs/
trading/crypto/freqtrade/user_data/models/
trading/crypto/freqtrade/user_data/freqaimodels/
trading/crypto/freqtrade/user_data/plot/
trading/crypto/freqtrade/user_data/notebooks/
trading/crypto/freqtrade/user_data/*.log
trading/crypto/freqtrade/user_data/*.pid
trading/crypto/freqtrade/user_data/*.sqlite*
trading/crypto/freqtrade/*.log
