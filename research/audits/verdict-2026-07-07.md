# Outside-Specialist Audit Verdict — 2026-07-07

Basis: `research/audits/independent-audit-20260707-140342.md` (666 modules, 2041 edges,
117 orphans, 11 cycles, 3 islands) + hands-on inspection. Owner granted full
replace-authority; no reuse bias applied.

## Connectivity summary
The brain is **densely and honestly connected** — `trading.state` (115 importers),
`core.node_protocol` (86), `trading.brain` (40) are healthy shared hubs, not fragile
chokepoints. Most of the 117 "orphans" are FALSE POSITIVES from dynamic dispatch:
Freqtrade loads its `lib_*` / `MlBridgeStrategy` strategies by config (not Python
import); `nodes.autoload` discovers node modules (incl. my new `nodes.loop_nodes`) by
directory sweep, not static import; `/status` endpoints are fetched on-click by the
Brain-Ops catalog. These are correct designs the static graph can't see.

## Real findings + verdicts

1. **RL naming collision — KEEP BOTH (complementary, not duplicate).**
   `trading/execution/rl_execution.py` (pre-existing) = RL order-SLICING to beat
   TWAP/Almgren-Chriss (execution-cost alpha). My W6 `trading/rl/exec_policy.py` = RL
   trade-DECISION (direction/SL/TP for PnL, vp2). Different layers of the stack; no
   merge. Action taken: none needed; documented here to prevent future confusion.

2. **Built-but-unwired on-chain whale feed — WIRED TODAY.**
   `trading/altdata/onchain.py` (free keyless fear&greed + BTC tx-count whale proxy +
   mempool composite) had an endpoint but fed no decision. Now a flag-guarded W4 scout
   (`maya-onchain`, macro BTC vote, 30-min cache) so it joins the consensus without
   violating the UI-only-data directive.

3. **goal↔journal import cycle (W1) — BENIGN, left as-is.**
   `trading.goal` imports `TradeJournal` inside `scoreboard()`; `journal.record()`
   imports `goal` inside the function. Both are FUNCTION-LEVEL lazy imports → no
   import-time deadlock, no runtime cost beyond first call. Breaking it (post-record
   hook) is cosmetic; not worth the indirection.

4. **`/api/trading/brain/decisions${query` "broken UI call" — FALSE POSITIVE.**
   The heuristic choked on a JS template literal; the route IS defined
   (`brain/decisions` appears in the defined list). No action.

5. **INDEX.md drift (377 "stale") — TOOL ARTIFACT, not real.**
   The audit's disk scan strips a path prefix (`_catalog/…`, `_cognition/…`) that
   doesn't match `gen_index`'s real paths (`trading/strategy/library/catalog/…`).
   `tools/gen_index.py` ran clean today and IS the source of truth. No action.

6. **`trading.advintel`, `trading.altdata` (news half), `trading.alerts.bot` — PARKED,
   not dead.** Real subsystems behind their own flags; candidates for #16 invent-beyond
   (e.g. advintel insider-style signals feeding the NSE scouts once the UI crawl reaches
   those pages). Left for the invent-beyond proposal, not deleted.

## SOTA keep-vs-replace scan (no reuse bias)
- **Strategy gate (CPCV + Deflated-Sharpe + FWER + my new leak tripwire)** — KEEP.
  This is at/above institutional SOTA; nothing better to swap in.
- **UQ (conformal CPS+ACI / crepes)** — KEEP; conformal is the current best calibrated-UQ
  approach for this setting.
- **RL (SB3 PPO)** — KEEP; PPO is the standard, well-tested on-policy baseline. A future
  upgrade worth benchmarking (not replacing yet): recurrent PPO / decision-transformer for
  the exec policy — noted for #16.
- **Memory (HippoRAG + A-MEM + FinMem episodes)** — KEEP; current SOTA associative stack.
- **Data path** — the owner's UI-only pivot IS the replacement (eyes over free-API poll);
  in flight via the governor.

## Verdict
No rip-and-replace warranted today — the architecture is sound and the "problems" are
mostly dynamic-dispatch blind spots in a static analyzer. The one genuine gap (an
unwired real signal source) is now wired. Upgrade candidates (recurrent-PPO exec,
advintel/altdata activation) are logged for the invent-beyond proposal.
