# Zero-evidence child + pseudo-obs blend = silent mass demotion
Blending a rich cumulative parent into a child bucket with n=0 caps effective n at the
borrow constant k (24) — below min_n (30) — so EVERY proven source became "unproven" the
moment day-buckets were cold. Rule: a hierarchy level with zero evidence hands to its parent
VERBATIM; only levels with some evidence blend. Caught by the pre-existing decide() tests.
