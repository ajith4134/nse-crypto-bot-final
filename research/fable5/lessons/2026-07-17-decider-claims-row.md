# The decider's own ledger row measures the FUSION, not a lens
learned_direction's recorded claims scored 0.393 clean while its best inputs scored 0.66 —
the one row that indicts the weighting itself. Primary mission scoreboard: learned_direction
vs learned_direction_ctl (the frozen pre-mission decider on a deterministic 20% split).
