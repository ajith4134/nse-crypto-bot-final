# Direction-Equation Quest — OWNER REQUIREMENTS (captured verbatim 2026-07-11)

> Saved BEFORE any prompt-upgrade, at the owner's explicit instruction: "save all the important
> words and meaning so when you convert it into a 10-star prompt that meaning or any of my
> requirements is [not] lost or our goal got changed." This file is the source of truth — the
> 10-star prompt and all execution must preserve EVERY item below without narrowing or drifting.

## Owner's message (normalized, meaning preserved)
Research and find the BEST data + equation to RELIABLY decide a symbol's price DIRECTION. Approach
it like an **equation generator**: figure out how many variables, and — mathematically — how to
FIND the equation from every known (input → output) data pair; then USE that equation, where the
output is unknown, to compute the unknown output (the direction) from the inputs. First find ALL
the data that drives a price / the movement of a symbol or stock. Research ONLINE, hard and
thoroughly, what that price/direction-driving data actually is (what the pros do). Once we have all
the influencing data, TRY ALL possible combinations / theories / equations to create the single
best, reliable feature/equation for direction. Read and SEARCH ALL related projects — even remotely
related — pull them ALL in, ADD our already-created features, then MUTATE and EVOLVE it into
something MORE ULTRA-ADVANCED and RELIABLE.

## Extracted asks (ALL must be honored — breadth is a requirement)
1. RESEARCH (hard, thorough, ONLINE) the best data + equation to reliably decide DIRECTION.
2. Treat it as an EQUATION-GENERATOR / SYMBOLIC-REGRESSION / SYSTEM-IDENTIFICATION problem:
   from known input→output pairs, discover the equation (variable count + functional form); then
   apply it to predict the UNKNOWN output (direction) from live inputs.
3. Find ALL the data/FORCES that actually drive a symbol's price & movement (the true inputs).
4. Try ALL possible COMBINATIONS / THEORIES / EQUATIONS → the single best reliable direction eq.
5. Read + search ALL RELATED PROJECTS, even remotely related — reuse them all.
6. ADD our EXISTING created features (already built in this project) into the pool.
7. MUTATE + EVOLVE the whole thing into something ultra-advanced and reliable (self-improving).
8. SAVE the meaning first (this file) so the 10-star prompt preserves 100% and the goal is intact.

## Glossary — the owner's key words → precise meaning (do NOT let these drift)
- "equation generator" → SYMBOLIC REGRESSION / system identification: discover the formula from
  data (candidates: PySR, gplearn, Operon, AI-Feynman, SINDy).
- "how many variables … how we find the equation in every known inputs and output" → determine the
  minimal variable set + derive/fit the functional form on known (X → direction) pairs.
- "use the equation … find that unknown output" → deploy the discovered equation to predict the
  direction (unknown) from current inputs.
- "all the data that drives a price or movement" → the COMPLETE set of price-driving factors:
  order flow / positioning / liquidity / funding-basis / on-chain / macro / sentiment / microstructure.
- "try all the possible combinations theories equations" → exhaustive feature+model search
  (combinatorial + theory-driven), not one hand-picked model.
- "best reliable feature or equation to decide the direction" → deliverable = ONE robust direction
  predictor, measured OUT-OF-SAMPLE (Truth Ledger) > coin-flip, robust across regimes/horizons.
- "read and search all the related projects even remotely related" → broad OSS + literature survey,
  reuse-first; nothing excluded for being tangential.
- "mutate and evolve it into something more ultra advanced" → evolutionary/genetic program search
  over features+equations; self-improving; keeps the best.
- "reliable" → the hard bar. Grounded in today's measurement: current direction is 47.4% aggregate
  (coin-flip), 4h=55% / exit=37% — see research/direction-accuracy-diagnosis-2026-07-11.md.

## Non-negotiables (project rules that still apply)
- Reuse-first but prefer the genuinely-best ([[prefer-best-not-just-existing]]); ask-to-install ok.
- Measure everything against the Truth Ledger (out-of-sample), never in-sample overfit.
- Paper-lab: exploration allowed; but the deliverable must be a MEASURED-reliable direction eq.
- PROPOSE→APPROVE for the build (ai-scientist), but the RESEARCH + capture happen now.
