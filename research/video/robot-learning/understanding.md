# Understanding: "Neural Nets Robot is Learning to Trade" (15.42 s, 2 frames, no speech)

## Summary

The clip is a silent 15-second screen recording of a Windows desktop trading-strategy application evolving a neural-network trading strategy on BITMEX data via a genetic/neuro-evolution process. Both frames show the same layout: an equity-curve chart climbing steadily up-and-right with a shaded "← Train →" region marking the in-sample training window (roughly the first ~24 units of the x-axis, with performance continuing out-of-sample beyond it), a small red/green histogram inset ("label1", apparently a per-bucket P&L distribution), a "Neural Net" panel on the right, a bottom "Trades" strip of green/red per-trade P&L bars with a stats headline, and a title bar reporting round-trip trade counts and average $ per trade. Between frame 1 (t=0:00) and frame 2 (t=0:10) the run visibly improves — the title bar goes from "Roundtrip trds: 2370 $1.76 per trd" to "2288 $2.61 per trd", the headline from "59% profitable. 1389 wins 979 losses. Average win 1.2x loss. 14.25 vs -11.73" to "63% profitable. 1443 wins 845 losses. Average win 1.4x loss. 14.41 vs -10.52", and frame 2 reveals the evolution internals: "Strategy_MM BITMEX", fitness function "FITNESS_PROFIT_PER_TRADE_TIMES_MINIMIZE_UNDERWATER", "Generations: 642 / Cur Gen: 642", offspring counters "Asexual: 49400, Sexual: 1960, Interspecies: 0, Random: 2984, Input Switch: 11969", and a rendered network graph labeled "Neural Net Complexity 90" with a top row of ~30 numbered input nodes and a sparse, irregular (NEAT-like) topology of red/blue weighted connections down to bottom output nodes. The message of the video is simply: a robot/GA is evolving neural-net trading strategies live, and the metrics get better as it learns. There is no audio, no code, and no further explanation — everything below is what is literally visible.

## Requirements (as redesign requirements for our trading NN)

- **REQ-RBT-01** [00:00, f_0000_000; 00:10, f_0010_001] The system trains/evolves a neural-net trading strategy continuously in a loop, with live-updating metrics between iterations (the whole 15 s clip is one such learning session where stats improve from frame 1 to frame 2).
- **REQ-RBT-02** [00:10, f_0010_001] Strategy evolution is genetic/neuro-evolutionary: population over generations ("Generations: 642, Cur Gen: 642") with multiple mutation/crossover operators tracked by count — "Asexual: 49400, Sexual: 1960, Interspecies: 0, Random: 2984, Input Switch: 11969". Implies: asexual mutation, sexual crossover, interspecies crossover (speciation), random-new-individual injection, and an "input switch" operator that swaps which market inputs feed the net.
- **REQ-RBT-03** [00:10, f_0010_001] Explicit named fitness function shown on-screen: `FITNESS_PROFIT_PER_TRADE_TIMES_MINIMIZE_UNDERWATER` — i.e. optimize profit-per-trade multiplied by a drawdown-minimization (underwater-curve) term, not raw total profit.
- **REQ-RBT-04** [00:10, f_0010_001] Strategy is named/segmented per market: "Strategy_MM BITMEX" (market-making-style strategy bound to the BITMEX crypto venue). Strategies carry an identity tied to venue/instrument.
- **REQ-RBT-05** [00:00 f1; 00:10 f2] The evolved network has variable, evolvable topology: frame 2 renders "Neural Net Complexity 90" — a NEAT-style irregular graph with ~30 numbered input nodes across the top (…30 visible at right edge), interior numbered nodes (e.g. 467, 3293, 3976, 2943, 3760, 3157 — node IDs grow, indicating nodes added over evolution), sparse red/blue edges (visibly two signed weight colors, varying thickness), and bottom nodes numbered ~31–39. Frame 1's "Neural Net" panel is empty/blank at t=0, i.e. the visualization (or the net) populates as learning proceeds. Network complexity is itself a measured, displayed scalar (90).
- **REQ-RBT-06** [00:00 f1; 00:10 f2] Backtest visualization: cumulative equity curve (dark line) overlaid with per-period detail (light-green underlay/spikes), y-axis in P&L units (f1: 0–8000; f2 rescaled 0–10+, suggesting normalized or per-trade units), x-axis 0–20+ (time buckets).
- **REQ-RBT-07** [00:00 f1; 00:10 f2] Train/test split shown honestly on the chart: a shaded horizontal band labeled "← Train →" covers only the left portion of the timeline; the equity curve continues rising beyond it, demonstrating out-of-sample performance visually.
- **REQ-RBT-08** [00:00 f1; 00:10 f2] Headline trade-quality stats line, exact format: "59% profitable. 1389 wins 979 losses. Average win 1.2x loss. 14.25 vs -11.73" (f1) → "63% profitable. 1443 wins 845 losses. Average win 1.4x loss. 14.41 vs -10.52" (f2). Required metrics: win-rate %, win count, loss count, avg-win/avg-loss ratio, average win value vs average loss value.
- **REQ-RBT-09** [00:00 f1; 00:10 f2] Per-trade P&L strip ("Trades" tab): a dense bar chart of every trade's P&L, green above zero / red below, spanning the whole test period — lets a human eyeball loss clusters and tail losses at a glance.
- **REQ-RBT-10** [00:00 f1; 00:10 f2] Title-bar cost-aware summary: "Roundtrip trds: 2370 $1.76 per trd" → "Roundtrip trds: 2288 $2.61 per trd" — round-trip trade count and average dollar profit per round-trip as the always-visible top-line number (per-trade economics, not just total P&L).
- **REQ-RBT-11** [00:00 f1; 00:10 f2] Inset histogram ("label1"): red bars on the left tail, green bars on the right, i.e. a distribution view (per-trade or per-bucket P&L distribution) shown alongside the equity curve; its shape shifts greener between frames as the strategy improves.
- **REQ-RBT-12** [00:00→00:10, both frames] Learning direction requirement implied by the delta: evolution should improve trade quality even at the cost of fewer trades (trades fell 2370→2288 while $/trade rose $1.76→$2.61, win rate 59%→63%, avg-loss shrank -11.73→-10.52) — consistent with the underwater-minimizing fitness in REQ-RBT-03.

## Open questions

- The video is extremely thin: no audio (transcript: "No speech detected"), no code, only 2 frames of one GUI. Everything above is inferred from static screenshots of what appears to be a custom C#/WinForms neuro-evolution trading app (resembles NEAT-based "robot trading" hobby projects).
- What exactly the "label1" histogram bins are (per-trade P&L? monthly P&L?) is not stated.
- Units/timeframe of the x-axis (days? thousands of bars?) and of the y-axis rescale between frames (8000 → 10) are unexplained — possibly total-$ vs $-per-trade views.
- Whether "Interspecies: 0" means the operator is disabled or just never selected is unknown.
- The exact math of `FITNESS_PROFIT_PER_TRADE_TIMES_MINIMIZE_UNDERWATER` (how "minimize underwater" is scored) is not shown.
- Whether the shown run is walk-forward (train band slides) cannot be determined from 2 frames.
